#include <stdio.h>

main()
{
    printf("Hello, world \d");

    //Providing a different escape sequence than the ones provided (\t, \b, \n, \\, \")
    //results in a warning from compiler that identifes it as an unknown escape sequence
}